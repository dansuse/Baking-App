<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/iv_size"
    xmlns:tools="http://schemas.android.com/tools">
    <ImageView
        android:id="@+id/iv_recipe_picture"
        android:layout_width="@dimen/iv_size"
        android:layout_height="@dimen/iv_size"
        tools:src="@drawable/no_image_available"/>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:layout_marginLeft="@dimen/margin_medium"
        android:layout_marginStart="@dimen/margin_medium"

        android:orientation="vertical"
        android:layout_toRightOf="@id/iv_recipe_picture"
        android:layout_toEndOf="@id/iv_recipe_picture"
        android:layout_centerVertical="true">
        <TextView
            android:id="@+id/tv_recipe_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_sz_large"
            android:textStyle="bold"
            tools:text="Nutella Pie"/>
        <TextView
            android:id="@+id/tv_recipe_servings"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_sz_large"
            tools:text="Servings : 8"/>
    </LinearLayout>
</RelativeLayout>